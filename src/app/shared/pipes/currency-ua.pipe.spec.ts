import { CurrencyUaPipe } from './currency-ua.pipe';

describe('CurrencyUaPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyUaPipe();
    expect(pipe).toBeTruthy();
  });
});
